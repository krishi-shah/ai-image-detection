\documentclass[12pt,a4paper]{article}

% NOTE FOR OVERLEAF: Keep outputs/ relative to the project root (or adjust paths).
% Heatmap filenames use underscores (no spaces).

% ── Packages ──
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{placeins}
\usepackage{grffile}
\usepackage{array}
% If you later create a .bib file, uncomment these:
% \usepackage[backend=biber,style=ieee]{biblatex}
% \addbibresource{references.bib}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!70!black,
    citecolor=blue!70!black,
    urlcolor=blue!70!black,
}

% ── Header/Footer ──
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small AI-Generated Image Detection}
\fancyhead[R]{\small Final Report}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\setlength{\headheight}{14pt}

% ── Section formatting ──
\titleformat{\section}{\large\bfseries}{}{0em}{}[\titlerule]
\titleformat{\subsection}{\normalsize\bfseries}{}{0em}{}
\titlespacing*{\section}{0pt}{1.4em plus 0.3em minus 0.2em}{0.55em}
\titlespacing*{\subsection}{0pt}{1.0em plus 0.2em minus 0.1em}{0.4em}
\captionsetup{skip=8pt,belowskip=2pt}
\setlength{\textfloatsep}{12pt plus 3pt minus 3pt}
\setlength{\floatsep}{10pt plus 2pt minus 2pt}
\setlength{\intextsep}{10pt plus 2pt minus 2pt}
\preto{\section}{\FloatBarrier}

% ══════════════════════════════════════════════════════════════
\begin{document}

% Title page uses Alph so it does not collide with body page~1.
\pagenumbering{Alph}

% ── Title Page ──
\begin{titlepage}
\centering
\vspace*{2.5cm}
{\Huge\bfseries Final Report\par}
\vspace{1cm}
{\LARGE AI-Generated Image Detection\par}
\vspace{0.5cm}
{\large\itshape Evaluating Generalisation Across Generative Model Generations\par}
\vspace{2.5cm}

\begin{tabular}{rl}
\textbf{Author:}      & Krishi Rajeshkumar Shah (220968905) \\
\textbf{Supervisor:}   & Mona Nasery \\
\textbf{Course:}       & EECS 4080 --- Computer Science Project \\
\textbf{Institution:}  & York University \\
\textbf{Term:}         & Summer 2026 \\
\textbf{Date:}         & August 2026 \\
\textbf{Repository:}   & \url{https://github.com/krishi-shah/ai-image-detection} \\
\end{tabular}

\vfill
\end{titlepage}

\pagenumbering{roman}
\tableofcontents
\newpage
\pagenumbering{arabic}

% ══════════════════════════════════════════════════════════════
\section{Abstract}

AI-image detectors trained on older generative models may fail as newer architectures close the perceptual gap with real photographs. This project fine-tunes an EfficientNet-B3 on CIFAKE (Stable Diffusion v1.4), adds temperature scaling and Grad-CAM explainability, and evaluates transfer to StyleGAN, SD3/Flux, Midjourney~v6, GPT-4o, and Janus-Pro. In-distribution performance is strong (96.96\% accuracy, AUC 0.9971, ECE 0.0026). Cross-generator transfer is robust (94--97\% fake detection) except for GPT-4o (86.3\%). Frequency analysis, t-SNE, and Grad-CAM support a GPT-4o \textbf{feature-absence} story: the detector attends to reasonable locations but finds no Stable Diffusion v1.4 artefacts. Temperature scaling fitted in-distribution does not remain optimal under shift.

% ══════════════════════════════════════════════════════════════
\section{Introduction and Motivation}

AI-generated images have become increasingly difficult to distinguish from authentic photographs. While detection tools exist, they are predominantly trained and evaluated on datasets of images generated by older generative models. As newer architectures such as Stable Diffusion~3, Midjourney~v6, and GPT-4o produce increasingly realistic outputs, a critical question emerges: \textbf{do existing detectors still work, and if not, why?}

This project investigates the \emph{generalisation gap} in AI-generated image detection --- the degree to which a detector trained on one generation of synthetic images degrades when tested against images produced by newer, more capable models. The original hypothesis is that current detectors rely on low-level statistical artefacts specific to the architectures they were trained on, and that these artefacts are no longer reliably present in outputs from state-of-the-art generative models.

\medskip
\noindent\textbf{Research Question:} \emph{How well do AI-generated image detectors trained on current benchmark datasets generalise to images produced by next-generation generative models, and what accounts for the performance gap?}

\medskip
\noindent\textbf{Original hypothesis:} Detectors rely on low-level statistical artefacts specific to the training generator; these artefacts are weaker or absent in newer models, producing a measurable generalisation gap that widens with generator novelty.

% ══════════════════════════════════════════════════════════════
\section{Brief Literature Review}

\textbf{Wang et al.\ (2020)} trained a ResNet-50 on ProGAN images and showed it generalised surprisingly well across 11 other GAN architectures, primarily because data augmentation (blur, JPEG compression) forced the model to learn shared GAN fingerprints. However, this study predates diffusion models entirely.

\textbf{Bird and Lotfi (2024)} created the CIFAKE dataset (60K images) and demonstrated $>$92\% detection accuracy using standard architectures. They used LIME for explainability; this project replaces it with Grad-CAM for faster, more spatially coherent heatmaps.

\textbf{Corvi et al.\ (2023)} showed a clear generalisation gap: GAN-trained classifiers dropped to near-chance accuracy (50--60\%) on diffusion model outputs and vice versa. Their frequency analysis showed GAN and diffusion images occupy separate regions in feature space. That result motivates this project's cross-family test.

\textbf{Gragnaniello et al.\ (2021)} demonstrated that different GANs leave different fingerprints and that there is a trade-off between in-distribution accuracy and cross-generator transferability.

\textbf{Guo et al.\ (2017)} established that modern deep networks are poorly calibrated and proposed temperature scaling as a simple, effective post-hoc calibration method.

\textbf{Selvaraju et al.\ (2017)} introduced Grad-CAM for visual explanations of CNN decisions via gradient-weighted class activation mapping.

\medskip
\noindent\textbf{Research Gap:} No existing study combines (1) testing across five chronologically distinct generator families, (2) measuring calibration alongside accuracy, (3) using Grad-CAM comparatively across generator families, and (4) focusing on measuring the generalisation problem rather than proposing a new detector.

% ══════════════════════════════════════════════════════════════
\section{Datasets}

The primary training and evaluation dataset is \textbf{CIFAKE} (Bird \& Lotfi, 2024), downloaded from Kaggle. The 60{,}000 and 80{,}000 figures in this report are \emph{different quantities}, not a contradiction. Table~\ref{tab:accounting} is the single accounting.

\begin{table}[ht]
\centering
\caption{CIFAKE counts used in this project.}
\label{tab:accounting}
\small
\begin{tabular}{p{4.2cm}p{4.0cm}p{6.0cm}}
\toprule
\textbf{Quantity} & \textbf{Count} & \textbf{What it is} \\
\midrule
On disk & 120{,}000 & Kaggle archive: 100k \texttt{train/} + 20k \texttt{test/} \\
By class & 60{,}000 REAL + 60{,}000 FAKE & CIFAR-10 photos vs Stable Diffusion v1.4 \\
Paper / Kaggle card & often ``60{,}000'' & one class, or an older card --- \emph{not} this archive \\
80/20 of \texttt{train/} (seed 42) & \textbf{80{,}000 train} + 20{,}000 val & split of the 100k folder, not a different dataset \\
Held-out test & 20{,}000 & the separate \texttt{test/} folder \\
\bottomrule
\end{tabular}
\end{table}

\noindent 80k + 20k + 20k $= 120$k. Nothing is missing and nothing is double-counted. All experiments use these on-disk counts. Resolution is $32\times32$ (upscaled to $224\times224$). Classes: FAKE $=0$, REAL $=1$.

\begin{table}[ht]
\centering
\caption{Train / validation / test splits (verified balanced).}
\label{tab:splits}
\begin{tabular}{lrrrl}
\toprule
\textbf{Split} & \textbf{FAKE (0)} & \textbf{REAL (1)} & \textbf{Total} & \textbf{\% FAKE} \\
\midrule
Train      & 39,962 & 40,038 & 80,000 & 50.0\% \\
Validation & 10,038 &  9,962 & 20,000 & 50.2\% \\
Test       & 10,000 & 10,000 & 20,000 & 50.0\% \\
\bottomrule
\end{tabular}
\end{table}

Both REAL and FAKE images use the identical preprocessing pipeline --- they are loaded together via PyTorch's \texttt{ImageFolder}.

\subsection{Data Sources}

Table~\ref{tab:sources} lists every image source. Cross-generator fakes were streamed from HuggingFace (not GenImage); see sampling below.

\begin{table}[ht]
\centering
\caption{Image sources.}
\label{tab:sources}
\small
\begin{tabular}{p{3.6cm}p{4.2cm}p{6.4cm}}
\toprule
\textbf{Set} & \textbf{Role} & \textbf{Source} \\
\midrule
CIFAKE & train / val / test & Kaggle (Bird \& Lotfi, 2024) \\
StyleGAN & 300 FAKE & CommunityForensics (GAN) \\
SD3/Flux & 300 FAKE & Defactify Image Dataset \\
Midjourney v6 & 300 FAKE & Defactify Image Dataset \\
GPT-4o & 300 FAKE & GPT-ImgEval (Yejy53) \\
Janus-Pro & 300 FAKE & Janus-Pro-R1-Data (midbee) \\
CIFAKE \texttt{test/REAL} & 300 REAL per family & CIFAKE test folder \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Sampling Procedure}
\label{sec:sampling}

The generalisation download script streams each HuggingFace split, keeps matching rows until $3N$ are collected, shuffles with seed~42, and writes $N=300$ PNGs per family. The download is skipped if the folder already has $\geq 300$ images. The loader then pairs those fakes with 300 CIFAKE test reals via a seeded permutation. $N=300$ is enough to see GPT-4o versus the remaining families; it is not enough to rank Midjourney against StyleGAN.

Because half of each eval set is in-distribution CIFAKE REAL, \textbf{fake detection rate} (FAKE recall) is the primary out-of-distribution metric.

% ══════════════════════════════════════════════════════════════
\section{Methodology and Model Architecture}

\subsection{Architecture}

The detector is an \textbf{EfficientNet-B3} pretrained on ImageNet, loaded via the \texttt{timm} library. The original classifier head is replaced with a linear layer mapping the 1,536-dimensional feature vector to 2 output classes:

\begin{verbatim}
EfficientNet-B3 (ImageNet pretrained)
  +-- classifier: Linear(1536, 2)
\end{verbatim}

This is a binary classification model outputting logits for [FAKE, REAL], converted to probabilities via softmax.

\subsection{Label Mapping}

PyTorch's \texttt{ImageFolder} sorts class folders alphabetically, producing:
\begin{itemize}[noitemsep]
    \item \textbf{FAKE = class 0} (folder name: \texttt{FAKE})
    \item \textbf{REAL = class 1} (folder name: \texttt{REAL})
\end{itemize}
This mapping is consistent across all splits. The model's softmax output at index~0 gives $P(\text{FAKE})$ and at index~1 gives $P(\text{REAL})$. A sample is classified as FAKE if $P(\text{FAKE}) > 0.5$ (default threshold).

\subsection{Post-hoc Calibration}

After training, confidence calibration is applied using \textbf{temperature scaling} (Guo et al., 2017). A single scalar parameter $T$ is \emph{learned} (not set manually) on the validation set by minimising negative log-likelihood using L-BFGS optimisation:
\[
    \text{calibrated\_probs} = \text{softmax}(\text{logits} / T)
\]
The temperature is initialised at 1.5 and optimised for up to 50 L-BFGS iterations. The learned value was $T = 1.2189$, indicating the model was slightly overconfident ($T > 1$ softens the distribution).

\subsection{Visual Explainability}

\textbf{Grad-CAM} (Selvaraju et al., 2017) is integrated using the \texttt{pytorch-grad-cam} library. The target layer is the final convolutional head of EfficientNet-B3 (\texttt{model.conv\_head}), which provides the highest-level spatial features before the classifier.

% ══════════════════════════════════════════════════════════════
\section{Training Setup and Preprocessing}

\subsection{Hyperparameters}

A hyperparameter sweep over 6 combinations (3 learning rates $\times$ 2 weight decays, 3 epochs each) selected the pair with highest validation accuracy for the full 20-epoch run. Table~\ref{tab:hyperparams} lists the configuration.

\begin{table}[ht]
\centering
\caption{Training hyperparameters.}
\label{tab:hyperparams}
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Learning rates (sweep) & $3\times10^{-4}$, $10^{-4}$, $5\times10^{-5}$ \\
Weight decays (sweep)  & $10^{-4}$, $10^{-5}$ \\
Selected LR            & $\mathbf{3\times10^{-4}}$ (best 3-epoch val acc $0.9594$) \\
Selected weight decay  & $\mathbf{10^{-4}}$ \\
Epochs (full)       & 20 (best checkpoint: epoch 2) \\
Batch size          & 32 \\
Optimiser           & AdamW \\
Scheduler           & CosineAnnealingLR ($T_{\max} = 20$) \\
Loss function       & CrossEntropyLoss \\
Random seed         & 42 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Train/Validation/Test Split}

The CIFAKE \texttt{train/} folder is split 80/20 with seed~42; \texttt{test/} is held out. Counts are in Table~\ref{tab:splits} (80k + 20k + 20k $= 120$k).

\subsection{Checkpoint selection (why epoch 2)}
\label{sec:epoch2}

Training ran for the scheduled 20 epochs. After each epoch the checkpoint was overwritten only if \textbf{validation accuracy} improved. The best was \textbf{epoch 2} (val acc $0.9694$), saved as \texttt{outputs/checkpoints/best\_detector.pth}. CIFAKE is easy for ImageNet-pretrained EfficientNet-B3 --- strong SD~v1.4 texture at $32\times32$ --- so validation peaks early; later epochs would overfit. All evaluation loads this best checkpoint, not the last epoch.

\subsection{Preprocessing}

\textbf{Training transforms} (applied identically to both FAKE and REAL):
\begin{enumerate}[noitemsep]
    \item \texttt{Resize(224)} --- scale to model input size
    \item \texttt{RandomHorizontalFlip()} --- augmentation
    \item \texttt{RandomCrop(224, padding=8)} --- augmentation
    \item \texttt{ColorJitter} --- brightness/contrast/saturation $0.2$, hue $0.1$
    \item \texttt{ToTensor()} --- convert to $[0, 1]$ float tensor
    \item ImageNet \texttt{Normalize} --- mean $[0.485, 0.456, 0.406]$, std $[0.229, 0.224, 0.225]$
\end{enumerate}

\textbf{Validation and test transforms} (applied identically to both FAKE and REAL):
\begin{enumerate}[noitemsep]
    \item \texttt{Resize(256)}
    \item \texttt{CenterCrop(224)}
    \item \texttt{ToTensor()}
    \item ImageNet \texttt{Normalize} (same mean/std as training)
\end{enumerate}

\subsection{Verification: Identical Preprocessing for Both Classes}
\label{sec:preprocess}

Both REAL and FAKE images are loaded via a single \texttt{ImageFolder} call, which assigns the same transform pipeline to every image regardless of its class label. There is no class-conditional preprocessing --- both classes undergo identical resizing, augmentation, tensor conversion, and normalisation. This was explicitly verified by inspecting the data loader code and confirming that the \texttt{transform} argument is shared.

% ══════════════════════════════════════════════════════════════
\section{Experimental Results}

\subsection{Overall Performance}

The trained checkpoint (epoch 2, validation accuracy 0.9694; Section~\ref{sec:epoch2}) was evaluated on the 20,000-image CIFAKE test set. Results are summarised in Table~\ref{tab:overall}.

\begin{table}[ht]
\centering
\caption{Overall test-set performance.}
\label{tab:overall}
\begin{tabular}{lr}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
Test Accuracy                        & \textbf{96.96\%} \\
AUC-ROC                             & \textbf{0.9971} \\
ECE (before calibration, test set)   & 0.0026 \\
ECE (after calibration, test set)    & 0.0113 \\
Learned Temperature                  & 1.2189 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Per-Class Performance}

Both classes achieve similar precision, recall, and F1 --- there is no systematic imbalance (Table~\ref{tab:perclass}).

\begin{table}[ht]
\centering
\caption{Per-class classification report on CIFAKE test set.}
\label{tab:perclass}
\begin{tabular}{lcccc}
\toprule
\textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} & \textbf{Support} \\
\midrule
FAKE (class 0) & 0.9858 & 0.9529 & 0.9691 & 10,000 \\
REAL (class 1) & 0.9544 & 0.9863 & 0.9701 & 10,000 \\
\midrule
Accuracy       &        &        & 0.9696 & 20,000 \\
Macro avg      & 0.9701 & 0.9696 & 0.9696 & 20,000 \\
Weighted avg   & 0.9701 & 0.9696 & 0.9696 & 20,000 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Confusion Matrix}

\begin{table}[ht]
\centering
\caption{Confusion matrix on CIFAKE test set.}
\label{tab:confusion}
\begin{tabular}{lcc}
\toprule
 & \textbf{Predicted FAKE} & \textbf{Predicted REAL} \\
\midrule
\textbf{Actual FAKE} & 9,529 (TP) & 471 (FN) \\
\textbf{Actual REAL} & 137 (FP)   & 9,863 (TN) \\
\bottomrule
\end{tabular}
\end{table}

The positive class is \textbf{FAKE} (class 0): TP means generated images caught. That is the detector's job, not sklearn's default of treating class~1 as positive. Labels follow \texttt{ImageFolder} alphabetical order (FAKE$=0$, REAL$=1$), the same mapping used in training. The counts check: $9{,}529+471=10{,}000$, $137+9{,}863=10{,}000$, accuracy $(9{,}529+9{,}863)/20{,}000=96.96\%$, FAKE precision $9{,}529/(9{,}529+137)=0.9858$.

\begin{itemize}[noitemsep]
    \item FAKE correctly classified: 9{,}529 / 10{,}000 (95.29\%)
    \item REAL correctly classified: 9{,}863 / 10{,}000 (98.63\%)
    \item FAKE predicted REAL (false negatives): 471
    \item REAL predicted FAKE (false positives): 137
\end{itemize}

\subsection{ROC Curve}

The ROC curve (Figure~\ref{fig:roc}) shows near-perfect discrimination with AUC-ROC = 0.9971, confirming the model performs well across all possible decision thresholds.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/confusion_matrix.png}
    \caption{Confusion matrix.}
    \label{fig:confusion}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/roc_curve.png}
    \caption{ROC curve (AUC = 0.9971).}
    \label{fig:roc}
\end{subfigure}
\caption{In-distribution discrimination on the CIFAKE test set (20{,}000 images).}
\end{figure}

\subsection{Train/Validation/Test Balance Verification}
\label{sec:balance}

The dataset splits were verified to be balanced (Table~\ref{tab:splits}). The identical preprocessing pipeline is applied to both classes via \texttt{ImageFolder} (Section~\ref{sec:preprocess}). There is no class imbalance that could explain differential performance.

A few correctly classified FAKE samples in the Grad-CAM set had per-image confidence of 70--72\%, while REAL samples in that set were near 99--100\%. Those are individual confidence scores, not per-class accuracy. On the full test set both classes sit at ${\sim}$97\% F1 (Table~\ref{tab:perclass}). Lower confidence on those particular fakes is consistent with good calibration, not a systematic class bias.

\subsection{Predicted Probability Distributions}

Histograms of $P(\text{FAKE})$ for actual FAKE and actual REAL images show well-separated distributions (Table~\ref{tab:probdist}, Figure~\ref{fig:probhist}).

\begin{table}[ht]
\centering
\caption{Predicted probability statistics by true class.}
\label{tab:probdist}
\begin{tabular}{lcc}
\toprule
\textbf{True Class} & \textbf{Mean $P$(FAKE)} & \textbf{Median $P$(FAKE)} \\
\midrule
Actual FAKE & 0.9293 & 0.9983 \\
Actual REAL & 0.0244 & 0.0000 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.70\textwidth]{outputs/plots/probability_histograms.png}
\caption{Predicted probability distributions for FAKE and REAL images.}
\label{fig:probhist}
\end{figure}

\subsection{Threshold Sensitivity Analysis}

Varying the classification threshold from 0.40 to 0.70 (Table~\ref{tab:threshold}), all metrics remain above 91\%, demonstrating model robustness. The highest accuracy (97.22\%) occurs at threshold 0.40. As threshold increases, precision rises but recall drops --- the standard precision--recall trade-off.

Despite threshold 0.40 yielding the highest accuracy, the default of 0.50 is retained because the gap is marginal (97.22\% vs.\ 96.96\%, only 0.26\%).
A lower threshold increases false positives: precision drops from 0.9858 at 0.50 to 0.9635 at 0.40, meaning more real images are incorrectly flagged as AI-generated.
The 0.50 boundary represents the natural decision point where the model is more confident than not, making it more interpretable.
It is also more robust to shifts in class distribution at deployment time, where the real-to-fake ratio may differ from the balanced test set.
For applications that prioritise catching every AI image (e.g., content moderation), a lower threshold could be justified, but for a general-purpose detector the balanced default is preferable.

\begin{table}[ht]
\centering
\caption{Threshold sensitivity analysis.}
\label{tab:threshold}
\begin{tabular}{ccccc}
\toprule
\textbf{Threshold} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
\midrule
0.40          & 0.9722 & 0.9807 & 0.9635 & 0.9720 \\
0.45          & 0.9716 & 0.9839 & 0.9589 & 0.9712 \\
\textbf{0.50} & \textbf{0.9696} & \textbf{0.9858} & \textbf{0.9529} & \textbf{0.9691} \\
0.55          & 0.9665 & 0.9871 & 0.9454 & 0.9658 \\
0.60          & 0.9646 & 0.9894 & 0.9393 & 0.9637 \\
0.65          & 0.9605 & 0.9906 & 0.9297 & 0.9592 \\
0.70          & 0.9563 & 0.9922 & 0.9197 & 0.9546 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.70\textwidth]{outputs/plots/threshold_all_metrics.png}
\caption{Threshold sensitivity --- all metrics.}
\label{fig:threshold_all}
\end{figure}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/threshold_f1.png}
    \caption{F1 vs.\ threshold.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/threshold_precision_recall.png}
    \caption{Precision/recall vs.\ threshold.}
\end{subfigure}
\caption{Threshold sensitivity breakdown.}
\label{fig:threshold_detail}
\end{figure}

\subsection{Temperature Scaling and Calibration}

The temperature parameter $T$ is \emph{learned} from the validation set via L-BFGS optimisation of negative log-likelihood. Table~\ref{tab:calibration} shows the results.

\begin{table}[ht]
\centering
\caption{Calibration results (ECE = Expected Calibration Error).}
\label{tab:calibration}
\begin{tabular}{lccc}
\toprule
\textbf{Set} & \textbf{ECE Before} & \textbf{ECE After} & \textbf{Temperature} \\
\midrule
Validation (used to learn $T$) & 0.0031 & 0.0116 & 1.2189 \\
Test (held-out)                & 0.0026 & 0.0113 & 1.2189 \\
\bottomrule
\end{tabular}
\end{table}

The model was already very well-calibrated before temperature scaling (test ECE = 0.0026). Applying temperature scaling \emph{increased} ECE on both sets, meaning the single-parameter adjustment over-corrected an already well-calibrated model. Temperature scaling optimises negative log-likelihood, not ECE directly.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/reliability_pre_calibration.png}
    \caption{Before calibration (ECE = 0.0026).}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/reliability_post_calibration.png}
    \caption{After calibration (ECE = 0.0113).}
\end{subfigure}
\caption{Reliability diagrams before and after temperature scaling.}
\label{fig:reliability}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.70\textwidth]{outputs/plots/calibration_comparison.png}
\caption{Calibration comparison (before vs.\ after temperature scaling).}
\label{fig:calibration_comparison}
\end{figure}

% ══════════════════════════════════════════════════════════════
\section{Analysis and Discussion}

\subsection{Misclassified FAKE Images: Do They Appear Realistic?}

471 FAKE images were misclassified as REAL out of 10,000 (4.71\% false-negative rate). Figure~\ref{fig:misclassified} shows the most confidently misclassified samples (highest $P(\text{REAL})$).

\textbf{Visual inspection findings:} The misclassified FAKE images do appear notably more realistic than typical CIFAKE fakes. Specifically:
\begin{itemize}[noitemsep]
    \item \textbf{Fewer artefacts:} They lack the oversmoothing, colour banding, and texture repetition typical of SD~v1.4.
    \item \textbf{More realistic compositions:} The spatial layout, object placement, and colour distributions closely resemble natural photographs from CIFAR-10. There are no obvious structural inconsistencies (e.g., distorted edges, unnatural symmetry).
    \item \textbf{Natural colour distributions:} Unlike many CIFAKE fakes that exhibit slightly saturated or uniform colour palettes, these misclassified images have colour statistics closer to the real image distribution.
\end{itemize}

\textbf{Why does the model fail on these?} The model primarily relies on low-level texture artefacts specific to Stable Diffusion v1.4 (as confirmed by Grad-CAM analysis in Section~\ref{sec:gradcam}). When a generated image happens to have fewer of these artefacts --- either due to the specific prompt, random seed, or content type --- the model lacks sufficient signal to distinguish it from a real image. These images sit closest to the decision boundary in probability space ($P(\text{FAKE})$ near 0.5 rather than near 1.0).

This finding is consistent with Gragnaniello et al.\ (2021), who showed that some generated images fall closer to the decision boundary in feature space due to reduced generator-specific fingerprints. It also reinforces the project's central hypothesis: if even \emph{within} the training distribution some images evade detection by lacking artefacts, images from \emph{newer} generators that systematically produce fewer artefacts will be even harder to detect.

\begin{figure}[ht]
\centering
\includegraphics[width=0.78\textwidth]{outputs/plots/misclassified_fakes.png}
\caption{Most confidently misclassified FAKE images (predicted as REAL).}
\label{fig:misclassified}
\end{figure}

\subsection{Grad-CAM Findings}
\label{sec:gradcam}

Grad-CAM heatmaps reveal systematic differences in the model's attention:

\begin{itemize}[noitemsep]
    \item \textbf{REAL images:} Broad, diffuse attention across the image, focusing on fine-grained texture regions. Confidence uniformly high (99--100\%).
    \item \textbf{FAKE images:} More localised attention patterns. High-confidence predictions focus on regions with visible generation artefacts. Lower-confidence predictions (70--72\%) show more scattered activation, suggesting the model is uncertain about which regions contain artefacts.
\end{itemize}

This indicates the model primarily relies on \textbf{low-level texture artefacts specific to Stable Diffusion v1.4}. This finding directly supports the project's hypothesis: the detector has learned distribution-specific features that may not transfer to newer generators.

\begin{figure}[ht]
\centering
\includegraphics[width=0.82\textwidth]{outputs/plots/gradcam_comparison.png}
\caption{Grad-CAM comparison --- FAKE vs.\ REAL images.}
\label{fig:gradcam_comparison}
\end{figure}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/gradcam_fake.png}
    \caption{Grad-CAM on FAKE images.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/gradcam_real.png}
    \caption{Grad-CAM on REAL images.}
\end{subfigure}
\caption{Grad-CAM heatmaps by class.}
\label{fig:gradcam_classes}
\end{figure}

\subsubsection{Individual Grad-CAM Heatmaps}

Figure~\ref{fig:heatmaps_samples} shows four FAKE and four REAL CIFAKE examples. FAKE images show localised artefact hotspots; REAL images show diffuse texture attention.

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/FAKE_48_5.jpg}
\end{subfigure}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/FAKE_516.jpg}
\end{subfigure}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/FAKE_52_10.jpg}
\end{subfigure}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/FAKE_95_8.jpg}
\end{subfigure}

\vspace{0.4em}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/REAL_0066_7.jpg}
\end{subfigure}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/REAL_0124_10.jpg}
\end{subfigure}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/REAL_0504_5.jpg}
\end{subfigure}
\begin{subfigure}[b]{0.24\textwidth}
    \includegraphics[width=\textwidth]{outputs/heatmaps/REAL_0998_9.jpg}
\end{subfigure}
\caption{CIFAKE Grad-CAM examples. Top: FAKE. Bottom: REAL.}
\label{fig:heatmaps_samples}
\end{figure}

\subsection{Calibration Discussion}

The model achieved an ECE of 0.0026 before calibration, which is remarkably low --- when the model says it is 95\% confident, it is correct approximately 95\% of the time. Temperature scaling learned $T = 1.2189$, confirming slight overconfidence as expected for modern deep networks (Guo et al., 2017). However, because the pre-calibration ECE was already so low, applying temperature scaling actually \emph{increased} ECE slightly to 0.0113. This is a known edge case with temperature scaling --- it optimises negative log-likelihood, not ECE directly, and on already well-calibrated models the correction can be counterproductive.

% ══════════════════════════════════════════════════════════════
\section{Cross-Generator Generalisation Study}
\label{sec:generalisation}

The core research contribution of this project is a systematic evaluation of the CIFAKE-trained detector on images from progressively newer generative models. Five families, 300 FAKE images each, were paired with 300 CIFAKE \texttt{test/REAL} images as in Table~\ref{tab:sources} and Section~\ref{sec:sampling}.

\subsection{Generalisation Results}

Table~\ref{tab:generalisation} presents the full cross-generator evaluation results.

\begin{table}[ht]
\centering
\caption{Cross-generator generalisation results.}
\label{tab:generalisation}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccccc}
\toprule
\textbf{Generator} & \textbf{Acc.} & \textbf{Fake Det.} & \textbf{AUC} & \textbf{F1} & \textbf{ECE} & \textbf{ECE} & \textbf{Drop} & \textbf{Drop} \\
 & & \textbf{Rate} & & \textbf{(FAKE)} & \textbf{(pre-T)} & \textbf{(post-T)} & \textbf{(abs)} & \textbf{(rel)} \\
\midrule
CIFAKE (baseline) & 0.9696 & --- & 0.9971 & --- & 0.0026 & 0.0113 & 0.0000 & 0.0\% \\
GPT-4o            & \textbf{0.9233} & \textbf{0.8633} & 0.9926 & 0.9184 & 0.0083 & 0.0209 & 0.0463 & \textbf{4.8\%} \\
Janus-Pro         & 0.9617 & 0.9400 & 0.9958 & 0.9608 & 0.0199 & 0.0334 & 0.0079 & 0.8\% \\
Midjourney v6     & 0.9617 & 0.9400 & 0.9955 & 0.9608 & 0.0338 & 0.0454 & 0.0079 & 0.8\% \\
SD3/Flux          & 0.9767 & 0.9700 & 0.9975 & 0.9765 & 0.0229 & 0.0363 & $-$0.0071 & $-$0.7\% \\
StyleGAN          & 0.9617 & 0.9400 & 0.9945 & 0.9608 & 0.0342 & 0.0514 & 0.0079 & 0.8\% \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/cross_generator_accuracy.png}
    \caption{Accuracy vs.\ CIFAKE baseline.}
    \label{fig:cross_gen_accuracy}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/cross_generator_auc.png}
    \caption{AUC-ROC (GPT-4o lowest at $0.9926$).}
    \label{fig:cross_gen_auc}
\end{subfigure}
\caption{Cross-generator accuracy and AUC-ROC.}
\end{figure}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/degradation_waterfall.png}
    \caption{Accuracy drop vs.\ CIFAKE.}
    \label{fig:degradation}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/confidence_distributions_by_generator.png}
    \caption{$P(\text{FAKE})$ by family.}
    \label{fig:confidence_dist}
\end{subfigure}
\caption{Degradation and confidence by generator family.}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.72\textwidth]{outputs/plots/ece_comparison_by_generator.png}
\caption{Calibration (ECE) under distribution shift --- before and after temperature scaling.}
\label{fig:ece_comparison}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.82\textwidth]{outputs/plots/gradcam_comparison_grid.png}
\caption{Grad-CAM failure analysis: false negatives by generator family.}
\label{fig:gradcam_grid}
\end{figure}

\subsection{Protocol Note}

Every REAL image in this evaluation is a $32\times32$ CIFAR upscale; every FAKE is a high-resolution image downscaled to the model input size. Because half of each eval set is in-distribution CIFAKE REAL, overall accuracy is inflated by easy reals --- \textbf{fake detection rate} (FAKE recall) is the primary out-of-distribution metric.

\subsection{Key Findings}

\begin{enumerate}[noitemsep]
    \item \textbf{GPT-4o is the only generator with meaningful degradation.} At 92.33\% accuracy (4.8\% relative drop) and only 86.33\% fake detection rate, nearly 14\% of GPT-4o images fooled the detector. All other generators remained at or above 96\% accuracy.

    \item \textbf{The autoregressive hypothesis was partially wrong.} The original hypothesis predicted that autoregressive generators (GPT-4o, Janus-Pro) would cause degradation because they generate images fundamentally differently from diffusion models. However, Janus-Pro (also autoregressive) performed at 96.17\% accuracy --- identical to StyleGAN and Midjourney. The gap is \emph{GPT-4o-specific}, not a general weakness against autoregressive generators.

    \item \textbf{Diffusion-to-diffusion transfer is strong.} SD3/Flux actually \emph{exceeded} the CIFAKE baseline ($-$0.7\% ``degradation'').

    \item \textbf{Calibration does not transfer.} The CIFAKE-fitted temperature ($T = 1.2189$) worsened ECE on all out-of-distribution sets (post-T ECE ranged from 0.0209 to 0.0514, compared to 0.0113 on CIFAKE). Future deployments should fit calibration per target distribution or use methods robust to distribution shift.
\end{enumerate}

% ══════════════════════════════════════════════════════════════
\section{GPT-4o Investigation}
\label{sec:gpt4o}

Given that GPT-4o was the sole generator with meaningful degradation, a follow-up investigation was conducted to determine \emph{why} the detector fails specifically on GPT-4o images. Three complementary analyses were performed at the frequency, representation, and attention levels.

\subsection{FFT Radial Power Spectrum}

Each image was converted to grayscale, a 2-D FFT was applied, and the radial average of the power spectrum was computed. The mean spectrum (with standard deviation bands) was plotted for 150 images per family (Figure~\ref{fig:fft_spectrum}).

This analysis tests whether GPT-4o images lack the frequency-domain artefacts characteristic of Stable Diffusion v1.4 outputs. If GPT-4o's spectral profile overlaps with CIFAKE REAL rather than CIFAKE FAKE, it would indicate that the detector's learned frequency-based features are absent from GPT-4o images.

\begin{figure}[ht]
\centering
\includegraphics[width=0.75\textwidth]{outputs/plots/gpt4o_investigation/radial_power_spectra.png}
\caption{Radial power spectrum comparison across generator families.}
\label{fig:fft_spectrum}
\end{figure}

\subsection{Feature-Space Visualisation (t-SNE)}

Penultimate-layer embeddings (1,536 dimensions) were extracted from the EfficientNet-B3 global average pooling layer using a forward hook. 150 images per family were processed, and the combined embeddings were projected to 2-D using t-SNE (perplexity=30, seed=42).

This reveals whether GPT-4o images cluster with CIFAKE REAL images (indicating the model's internal representation treats them as real-looking) or with CIFAKE FAKE images (indicating a threshold or calibration issue).

\begin{figure}[ht]
\centering
\includegraphics[width=0.75\textwidth]{outputs/plots/gpt4o_investigation/feature_space_tsne.png}
\caption{t-SNE projection of EfficientNet-B3 embeddings by generator family.}
\label{fig:tsne}
\end{figure}

\subsection{Quantitative Grad-CAM Metrics}

To move beyond qualitative heatmap inspection, three quantitative metrics were computed on the raw Grad-CAM activation maps for 100 FAKE images per family (Table~\ref{tab:gradcam_metrics}):

\begin{itemize}[noitemsep]
    \item \textbf{Activation entropy} --- high values indicate diffuse, unfocused attention.
    \item \textbf{Peak activation ratio} --- the fraction of total activation in the top 10\% of pixels; higher values indicate concentrated attention on specific artefacts.
    \item \textbf{Gini coefficient} --- measures inequality of activation distribution; higher values indicate more spatially concentrated attention.
\end{itemize}

\begin{table}[ht]
\centering
\caption{Grad-CAM attention metrics by generator family (FAKE images only).}
\label{tab:gradcam_metrics}
\begin{tabular}{lccc}
\toprule
\textbf{Family} & \textbf{Mean Entropy} & \textbf{Mean Peak Ratio} & \textbf{Mean Gini} \\
\midrule
CIFAKE FAKE    & \textbf{10.197} & \textbf{0.339} & \textbf{0.574} \\
GPT-4o         & 10.291 & 0.309 & 0.529 \\
Janus-Pro      & 10.355 & 0.295 & 0.498 \\
Midjourney v6  & 10.376 & 0.282 & 0.478 \\
SD3/Flux       & 10.380 & 0.282 & 0.488 \\
StyleGAN       & 10.319 & 0.317 & 0.518 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.75\textwidth]{outputs/plots/gpt4o_investigation/gradcam_entropy_comparison.png}
\caption{Grad-CAM entropy distribution by generator family.}
\label{fig:gradcam_entropy}
\end{figure}

\begin{figure}[ht]
\centering
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/gpt4o_investigation/gradcam_peak_ratio_comparison.png}
    \caption{Peak activation ratio.}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.48\textwidth}
    \includegraphics[width=\textwidth]{outputs/plots/gpt4o_investigation/gradcam_gini_comparison.png}
    \caption{Gini coefficient.}
\end{subfigure}
\caption{Grad-CAM spatial concentration metrics by generator family.}
\label{fig:gradcam_spatial}
\end{figure}

\subsection{Investigation Conclusions}

CIFAKE FAKE images show the most focused attention (lowest entropy 10.197, highest Gini 0.574), confirming the model knows precisely where to look for SD~v1.4 artefacts. However, GPT-4o's attention metrics are \emph{not dramatically different} from well-detected families: Midjourney and SD3/Flux have higher entropy (10.376--10.380) and lower peak ratios (0.282) than GPT-4o (10.291 entropy, 0.309 peak ratio), yet achieve 96--97\% accuracy.

This rules out the hypothesis that the model ``does not know where to look'' on GPT-4o images. Instead, the degradation is a \textbf{feature-absence problem}: the model attends to reasonable spatial locations but finds no recognisable artefacts to latch onto. GPT-4o's rendering pipeline simply does not produce the spectral fingerprints that Stable Diffusion v1.4 generates, leaving the detector without a learnable signal regardless of where it attends.

% ══════════════════════════════════════════════════════════════
\section{Interactive Demo}
\label{sec:demo}

A Gradio application (\texttt{app.py}) returns REAL/FAKE classification, temperature-scaled confidence, and a Grad-CAM overlay. Launch with \texttt{python app.py} (or \texttt{python app.py --share}). The live runbook is \texttt{reports/demo\_script.md}. Behaviour is covered by \texttt{tests/test\_app.py} as part of the 63-test suite.

The interface uses \textbf{curated one-click samples} --- CIFAKE Real, CIFAKE Fake, StyleGAN, Midjourney, and GPT-4o --- so the demo shows the detector on known study images.

% ══════════════════════════════════════════════════════════════
\section{Project Timeline}

\begin{table}[ht]
\centering
\caption{Milestone timeline.}
\label{tab:timeline}
\begin{tabular}{lll}
\toprule
\textbf{M} & \textbf{Period} & \textbf{Deliverable} \\
\midrule
1 & May 1--20 & Literature, repo, CIFAKE EDA \\
2 & May 21--Jun 15 & EfficientNet-B3 + temperature scaling \\
3 & Jun 16--Jul 5 & Grad-CAM, ECE, threshold analysis \\
4 & Jul 6--20 & Five-family eval and GPT-4o investigation \\
5 & Jul 21--Aug 10 & Gradio demo \\
6 & Aug 11--28 & Final report and presentation \\
\bottomrule
\end{tabular}
\end{table}

% ══════════════════════════════════════════════════════════════
\section{Discussion}

\begin{table}[ht]
\centering
\caption{Hypothesis assessment.}
\label{tab:hyp}
\begin{tabular}{>{\raggedright\arraybackslash}p{6.4cm}>{\raggedright\arraybackslash}p{7.4cm}}
\toprule
\textbf{Claim} & \textbf{Outcome} \\
\midrule
Detectors fail on newer generators in general & Partially supported --- only GPT-4o shows a meaningful drop; other families transfer at 94--97\% fake detection \\
Autoregressive generators are especially hard & Partially supported (GPT-4o is the hard case; Janus-Pro matches StyleGAN/Midjourney) \\
Failure due to missing training artefacts & Supported for GPT-4o (FFT, t-SNE, Grad-CAM) \\
Calibration transfers under shift & Refuted \\
\bottomrule
\end{tabular}
\end{table}

The primary finding is that transfer is strong except for GPT-4o. Frequency, embedding, and attention analyses support a \textbf{feature-absence} account of that gap.

\subsection{Why not a second dataset or four detector models}

The supervisor suggested a second benchmark (e.g.\ GenImage) and comparing several detector architectures. Neither was run. That is a design choice, not a missing download.

The research question is how a \textbf{CIFAKE-trained} detector behaves on \emph{newer} generators. Retraining or re-testing on another 32$\times$32 / mixed-SD set would not answer that. Milestone~4 originally named GenImage; it was not used because GenImage does not cover GPT-4o, Janus-Pro, or Midjourney~v6. Five streamed families (Table~\ref{tab:sources}) were the substitute.

One \textbf{strong ordinary} network (EfficientNet-B3) was used on purpose. If a standard detector fails, the failure is the data and the test, not a weak backbone. Four models $\times$ a second dataset is an architecture bake-off --- a different project --- and is listed as future work.

% ══════════════════════════════════════════════════════════════
\section{Limitations}

\begin{enumerate}[noitemsep]
    \item \textbf{Single-generator $32\times32$ training.} The model is trained exclusively on CIFAKE (Stable Diffusion v1.4) at native $32\times32$ resolution. Results may not generalise to other backbones or to detectors trained on higher-resolution mixed-generator data.

    \item \textbf{Sample size.} 300 images per family is enough to see GPT-4o versus the remaining families. It is not enough to rank Midjourney against StyleGAN.

    \item \textbf{CIFAKE-real pairing.} Overall accuracy is inflated by easy in-distribution reals. Fake detection rate is the primary out-of-distribution metric.

    \item \textbf{Calibration does not transfer.} Temperature scaling fitted on CIFAKE ($T = 1.2189$) worsened ECE on all out-of-distribution generator families. Do not treat those confidence bars as true probabilities.

    \item \textbf{Single architecture.} EfficientNet-B3 may rely on cues that other backbones do not.
\end{enumerate}

% ══════════════════════════════════════════════════════════════
\section{Conclusions and Future Work}

\begin{enumerate}[noitemsep]
    \item A CIFAKE-trained EfficientNet-B3 reaches \textbf{96.96\%} accuracy in-distribution with near-perfect AUC and excellent pre-calibration ECE.
    \item Cross-generator transfer is strong except for GPT-4o (\textbf{86.3\%} fake detection versus 94--97\% on the other families).
    \item GPT-4o is the hard case; FFT, t-SNE, and Grad-CAM support \textbf{feature absence}.
    \item Temperature scaling fitted in-distribution \textbf{does not} remain optimal under shift.
    \item An interactive Gradio demo presents the detector on curated study samples.
\end{enumerate}

\noindent\textbf{Future work:} train on multi-generator, higher-resolution data (e.g.\ GenImage); add hybrid spatial--frequency features; evaluate ensembles; refit calibration per deployment distribution.

% ══════════════════════════════════════════════════════════════
\section{Repository}

All code, notebooks, tests, and results are available at:\\
\url{https://github.com/krishi-shah/ai-image-detection}

\subsection*{Notebooks}
\begin{itemize}[noitemsep]
    \item \texttt{01\_setup\_and\_eda.ipynb} --- Environment setup, CIFAKE download and EDA
    \item \texttt{02\_baseline\_training.ipynb} --- Hyperparameter sweep and full training
    \item \texttt{03\_calibration.ipynb} --- Temperature scaling and thresholds
    \item \texttt{04\_gradcam.ipynb} --- Grad-CAM heatmaps
    \item \texttt{05\_generalisation\_eval.ipynb} --- Five-family evaluation
    \item \texttt{06\_gpt4o\_investigation.ipynb} --- FFT, t-SNE, Grad-CAM metrics
\end{itemize}

\subsection*{Source Modules}
\begin{itemize}[noitemsep]
    \item \texttt{src/model/detector.py} --- EfficientNet-B3 build, checkpoint I/O
    \item \texttt{src/evaluation/metrics.py} --- Accuracy, AUC, ECE, ROC
    \item \texttt{src/evaluation/calibration.py} --- Temperature scaling (L-BFGS)
    \item \texttt{src/evaluation/generalisation.py} --- Cross-generator eval and plots
    \item \texttt{src/explainability/gradcam.py} --- Heatmaps and comparison grids
    \item \texttt{src/analysis/frequency.py} --- FFT radial power spectra
    \item \texttt{src/analysis/embeddings.py} --- t-SNE of penultimate embeddings
    \item \texttt{src/analysis/gradcam\_metrics.py} --- Entropy, peak ratio, Gini
    \item \texttt{src/utils/data\_loader.py} --- CIFAKE and family loaders
    \item \texttt{app.py} --- Gradio demo (curated study samples)
\end{itemize}

\subsection*{Tests}
63 unit tests across 7 test modules, all passing --- covering model architecture, data transforms, evaluation metrics, calibration, Grad-CAM, generalisation, and the Gradio demo.

% ══════════════════════════════════════════════════════════════
\section*{References}

\begin{enumerate}[label={[\arabic*]}, noitemsep]
    \item Bird, J.J.\ \& Lotfi, A.\ (2024). CIFAKE: Image Classification and Explainable Identification of AI-Generated Synthetic Images. \textit{IEEE Access}, 12, 15642--15650.
    \item Corvi, R.\ et al.\ (2023). On the Detection of Synthetic Images Generated by Diffusion Models. \textit{ICASSP 2023}.
    \item Gragnaniello, D.\ et al.\ (2021). Are GAN Generated Images Easy to Detect? A Critical Analysis of the State of the Art. \textit{ICME 2021}.
    \item Guo, C.\ et al.\ (2017). On Calibration of Modern Neural Networks. \textit{ICML 2017}.
    \item Selvaraju, R.\ et al.\ (2017). Grad-CAM: Visual Explanations from Deep Networks via Gradient-based Localization. \textit{ICCV 2017}.
    \item Wang, S.\ et al.\ (2020). CNN-Generated Images Are Surprisingly Easy to Spot\ldots For Now. \textit{CVPR 2020}.
\end{enumerate}

\end{document}